import React from 'react'

const SingleOrgView = (props) => {
    return (
        <div>
            <h1>Single Org View id:{props.match.params.id}</h1>
        </div>
    )
}

export default SingleOrgView
